# Команды для работы с удаленными репозиториями

```sh
git push
```
эта команда позволяет перенести изменения на удалённый репозиторий

```sh
git pull
```
эта команда позволяет перенести изменения на локальный репозиторий